{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.16.3"
  },
  "scripts": {
    "setup": "node setup.js",
    "start": "npm run setup & npx json-server --watch db.json --port 4000 --delay 1000"
  }
}
